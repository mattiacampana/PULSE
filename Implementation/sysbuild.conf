# Disable the (deprecated) Nordic Partition Manager. With it off, sysbuild uses
# the DTS `fixed-partitions` layout defined in the board devicetree as the
# authoritative flash map. See boards/.../SensWear_nrf54l15_cpuapp.dts.
SB_CONFIG_PARTITION_MANAGER=n
